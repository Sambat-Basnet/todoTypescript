export interface Itasks{
    id:string
    value:string
    isComplete:boolean
}

export type typeOfArr = Array<Itasks>
export type daysArr = Array<string>
export type monthArr = Array<string>
export type newArr = Array<string>